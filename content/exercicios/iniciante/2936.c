#include <stdio.h>

int main() {

    int x, y, z, w, u; 
    scanf("%d %d %d %d %d", &x, &y, &z, &w, &u);

    printf("%d\n", x * 300 + y * 1500 + z * 600 + w * 1000 + u * 150 + 225);
    
    return 0;
}
